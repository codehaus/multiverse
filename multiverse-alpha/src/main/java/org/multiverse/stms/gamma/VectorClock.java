package org.multiverse.stms.gamma;

import org.multiverse.utils.TodoException;

public class VectorClock {

    public long getTime() {
        throw new TodoException();
    }

    public void tick() {
        throw new TodoException();
    }

    public boolean overlaps(VectorClock vectorClock) {
        throw new TodoException();
    }
}
